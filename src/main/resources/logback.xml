<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>[%d{HH:mm:ss}] %-5level %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Корневой логгер: INFO и выше -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>